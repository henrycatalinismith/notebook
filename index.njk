---
title: notebook
description: some writing
eleventyExcludeFromCollections: true
---
<!doctype html>
<html lang="en">
 <head>
  <meta charset="utf-8">
  <meta
   name="viewport" 
   content="width=device-width, initial-scale=1.0"
  />
  <meta
   name="og:title" 
   content="{{ description }}"
  />
  <meta
   name="og:description" 
   content="{{ description }}"
  />
  <title>{{ title }}</title>
  <style>
   :root {
    --f12: clamp(1rem, calc(1rem + (100vw - var(--margin) * 2 - 8rem) / 96), 2rem);
    --f20: clamp(1rem, calc(1rem + (100vw - var(--margin) * 2 - 8rem) / 48), 3rem);
    --f30: clamp(1rem, calc(1rem + (100vw - var(--margin) * 2 - 8rem) / 24), 4rem);
    --margin: calc(
     clamp(
      0rem,
      calc((100vw - 16rem) / 2),
      1rem
     ) - clamp(
      0rem,
      calc((100vw - 64rem) / 2),
      1rem
     )
    );
    --width: min(
     100vw,
     64rem
    );
    --burst: calc(
     clamp(
      0rem,
      calc((100vw - 32rem) / 1),
      4rem
     ) + var(--width)
    );
   }

   html {
    box-sizing: border-box;
   }

   *, *:before, *:after {
    box-sizing: inherit;
   }

   body {
    background-color: ghostwhite;
    color: darkslategray;
    display: flex;
    font-family: Charter, serif;
    justify-content: center;
    margin: 0;
   }

   a[href] {
    color: royalblue;
   }

   h1 {
    font-size: var(--f30);
    margin: 32px 0 16px 0;
   }

   h2 {
    font-size: var(--f20);
    margin: 32px 0 0 0;
   }

   h2 + p {
    margin-top: 0;
   }

   h1, h2 {
    color: deeppink;
    font-family: "Hoefler Text", serif;
   }


   main {
    max-width: var(--width);
   }

   article + article {
    margin-top: 64px;
   }

   p {
    font-size: var(--f12);
    line-height: 1.5em;
   }

   h1, h2, p {
    margin-left: var(--margin);
    margin-right: var(--margin);
   }
   
  </style>
 </head>
 <body>
  <main>
   <h1>
    {{ title }}
   </h1>

   <div>
    {%- for note in collections.all | reverse -%}
     <article>
      <h2>
       <a href="{{ note.data.permalink | url }}">
        {{ note.data.title }}
       </a>
      </h2>
      <p>{{ note.data.description }}</p>
     </article>
    {%- endfor -%}
   </div>
  </main>
 </body>
</html>